# Placeholder Dockerfile for audit-service
FROM golang:1.21-alpine AS builder
WORKDIR /app
# Build steps will be added during implementation

FROM alpine:latest
WORKDIR /app
# Runtime configuration will be added during implementation
